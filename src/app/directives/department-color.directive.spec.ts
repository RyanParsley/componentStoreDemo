import { DepartmentColorDirective } from './department-color.directive';

describe('DepartmentColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DepartmentColorDirective();
    expect(directive).toBeTruthy();
  });
});
